<UserControl x:Class="HRToolkit.Wpf.Views.WorkspaceDrawerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="320">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="工作区资料库" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMainBrush}" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="刷新" Style="{StaticResource SecondaryButtonStyle}" 
                        Padding="8,4" FontSize="11" Command="{Binding RefreshCommand}" />
            </Grid>
        </Border>

        <!-- Drag and Drop Target -->
        <Border Grid.Row="1" Margin="12" Height="70" Background="#F8FAFC" BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" CornerRadius="6" AllowDrop="True"
                Drop="DropArea_Drop" DragOver="DropArea_DragOver">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="拖拽文件到此处快速保存" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMainBrush}" HorizontalAlignment="Center" />
                <TextBlock Text="自动存入当前项目共用资料目录" FontSize="10" Foreground="{StaticResource TextMutedBrush}" Margin="0,2,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- Files List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Files}" Margin="12,0,12,12">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="Medium" Foreground="{StaticResource TextMainBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Text="{Binding RelativePath}" FontSize="10" Foreground="{StaticResource TextMutedBrush}" TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding FormattedSize}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
