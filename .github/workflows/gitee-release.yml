name: Publish GitHub source release to Gitee

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Published GitHub Release tag (v<SemVer>)
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: gitee-source-release-${{ inputs.tag }}
  cancel-in-progress: false

jobs:
  mirror-release:
    name: Publish source archives and checksum
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      TAG: ${{ inputs.tag }}
      GITHUB_REPOSITORY: ${{ github.repository }}
      GITEE_REPOSITORY: optimistic-little-sunspot/hr-toolkit
      GITEE_USERNAME: optimistic-little-sunspot
    steps:
      - name: Check out latest release tooling from main
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          ref: main

      - name: Check out requested tagged source for version validation
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 1
          ref: ${{ inputs.tag }}
          path: tagged-source

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.12"

      - name: Validate requested tag and source version
        shell: bash
        run: |
          set -euo pipefail
          VERSION="${TAG#v}"
          python scripts/generate_release_metadata.py \
            --version "${VERSION}" \
            --tag "${TAG}" \
            --project-version-file tagged-source/hr_toolkit/__init__.py \
            --check-only
          echo "SOURCE_COMMIT=$(git rev-parse HEAD)" >> "${GITHUB_ENV}"
          echo "VERSION=${VERSION}" >> "${GITHUB_ENV}"

      - name: Require Gitee sync credential
        shell: bash
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "${GITEE_TOKEN}" ]]; then
            echo "Missing required GitHub Actions Secret: GITEE_TOKEN" >&2
            exit 1
          fi

      - name: Download and validate checksum metadata only
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$(gh release view "${TAG}" --repo "${GITHUB_REPOSITORY}" --json isDraft --jq .isDraft)" = "false"
          mkdir -p gitee-release-metadata
          gh release download "${TAG}" \
            --repo "${GITHUB_REPOSITORY}" \
            --pattern "SHA256SUMS.txt" \
            --dir gitee-release-metadata
          python scripts/publish_gitee_release.py \
            --version "${VERSION}" \
            --tag "${TAG}" \
            --repository "${GITEE_REPOSITORY}" \
            --target-commitish "$(git rev-parse "${TAG}^{commit}")" \
            --assets-dir gitee-release-metadata \
            --source-metadata-only \
            --dry-run

      - name: Fast-forward source and annotated tag to Gitee
        shell: bash
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
          GIT_TERMINAL_PROMPT: "0"
        run: |
          set -euo pipefail
          ASKPASS_PATH="${RUNNER_TEMP}/gitee-askpass.sh"
          cat > "${ASKPASS_PATH}" <<'SH'
          #!/bin/sh
          case "$1" in
            *Username*) printf '%s\n' "${GITEE_USERNAME}" ;;
            *Password*) printf '%s\n' "${GITEE_TOKEN}" ;;
            *) exit 1 ;;
          esac
          SH
          chmod 700 "${ASKPASS_PATH}"
          export GIT_ASKPASS="${ASKPASS_PATH}"

          git remote add gitee "https://gitee.com/${GITEE_REPOSITORY}.git"
          git fetch origin refs/heads/main:refs/remotes/origin/main
          python scripts/gitee_git.py fetch gitee refs/heads/main:refs/remotes/gitee/main

          TAG_COMMIT="$(git rev-parse "${TAG}^{commit}")"
          test "$(git rev-parse refs/remotes/origin/main)" = "${SOURCE_COMMIT}"
          git merge-base --is-ancestor "${TAG_COMMIT}" "${SOURCE_COMMIT}"
          git merge-base --is-ancestor refs/remotes/gitee/main "${SOURCE_COMMIT}"

          REMOTE_TAG="$(python scripts/gitee_git.py ls-remote gitee "refs/tags/${TAG}" | awk '{print $1}')"
          LOCAL_TAG="$(git rev-parse "refs/tags/${TAG}")"
          if [[ -n "${REMOTE_TAG}" && "${REMOTE_TAG}" != "${LOCAL_TAG}" ]]; then
            echo "Gitee tag ${TAG} already exists at a different object" >&2
            exit 1
          fi

          python scripts/gitee_git.py push --atomic gitee \
            "${SOURCE_COMMIT}:refs/heads/main" \
            "refs/tags/${TAG}:refs/tags/${TAG}"

      - name: Create and verify Gitee source release
        shell: bash
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          set -euo pipefail
          python scripts/publish_gitee_release.py \
            --version "${VERSION}" \
            --tag "${TAG}" \
            --repository "${GITEE_REPOSITORY}" \
            --target-commitish "$(git rev-parse "${TAG}^{commit}")" \
            --assets-dir gitee-release-metadata \
            --source-metadata-only \
            --upload-transport urllib \
            --timeout 60
