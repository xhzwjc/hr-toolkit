{
  "name": "hr-toolkit",
  "private": true,
  "version": "0.5.2",
  "scripts": {
    "release": "sh -c 'if [ -x .venv/bin/python ]; then exec .venv/bin/python scripts/release.py \"$@\"; else exec python3 scripts/release.py \"$@\"; fi' --"
  }
}
