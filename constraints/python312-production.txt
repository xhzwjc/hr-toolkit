# Production dependency set verified by the Windows and macOS Python 3.12
# release jobs. Keep requirements.txt/pyproject.toml as supported ranges for
# development; all Python 3.12 CI and packaging jobs resolve through this file
# so an upstream release cannot silently change a production build.
altgraph==0.17.5
backports.zstd==1.7.0
brotli==1.2.0
certifi==2026.7.22
cffi==2.1.1
colorama==0.4.6; platform_system == "Windows"
coloredlogs==15.0.1; platform_system == "Darwin" and platform_machine == "x86_64"
et_xmlfile==2.0.0
flatbuffers==25.12.19
humanfriendly==10.0; platform_system == "Darwin" and platform_machine == "x86_64"
inflate64==1.0.4
macholib==1.16.4; platform_system == "Darwin"
mpmath==1.3.0; platform_system == "Darwin" and platform_machine == "x86_64"
multivolumefile==0.2.3
# PySide6/Shiboken 6.6 is compiled against NumPy 1.x.  NumPy 2 emits an ABI
# crash warning while importing the desktop renderer, so keep the final
# Python 3.12 NumPy 1.x line. Intel macOS uses the last verified macOS 10.15
# OpenCV wheel and a macOS 11 universal2 ONNX Runtime wheel; this avoids the
# much larger x86_64 wheels while preserving RapidOCR's supported API surface.
# scripts/prepare_macos_x64_runtime.py replaces only Intel macOS NumPy with a
# source-equivalent no-external-BLAS build so the verified DMG stays below the
# release attachment limit. Other platforms keep the ordinary upstream wheel.
numpy==1.26.4
onnxruntime==1.17.3; platform_system == "Darwin" and platform_machine == "x86_64"
onnxruntime==1.29.0; platform_system != "Darwin" or platform_machine != "x86_64"
opencv-python==4.5.5.64; platform_system == "Darwin" and platform_machine == "x86_64"
opencv-python==4.10.0.84; platform_system != "Darwin" or platform_machine != "x86_64"
openpyxl==3.1.5
packaging==26.3
pefile==2024.8.26; platform_system == "Windows"
pillow==12.3.0
pip==26.2
protobuf==7.36.0
psutil==7.2.2
py7zr==1.1.3
pybcj==1.0.8
pyclipper==1.4.0
pycparser==3.0
pycryptodomex==3.23.0
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
pyside6-essentials==6.6.3.1
pypdf==6.16.1
pyppmd==1.3.1
pywin32==312; platform_system == "Windows"
pywin32-ctypes==0.2.3; platform_system == "Windows"
pyyaml==6.0.3
rapidocr_onnxruntime==1.4.4
setuptools==84.0.0
shiboken6==6.6.3.1
shapely==2.1.2
six==1.17.0
sympy==1.14.0; platform_system == "Darwin" and platform_machine == "x86_64"
texttable==1.7.0
tqdm==4.70.0
unrar2-cffi==0.5.0
xlrd==2.0.2
